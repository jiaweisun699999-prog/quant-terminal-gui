rich>=13.7.0
# AkShare 作为“能抓就抓”的可选数据源；抓取失败会回退到手工输入。
akshare>=1.14.0
# PyInstaller 打包后显式指定 CA，避免 HTTPS 校验失败
certifi>=2024.2.2

# Terminal UI (frontend-like experience in console)
textual>=0.76.0

# Desktop GUI
PySide6>=6.7.0

# Packaging (optional)
pyinstaller>=6.0.0

